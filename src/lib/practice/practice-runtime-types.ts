/** Persisted in `LearningSession.revisitMetaJson.practiceRuntime` for dual-axis guided practice. */
export type PracticeRuntimeMeta = {
  dualAxis: boolean;
  mode?: string;
  skill?: string;
  moduleKey?: string;
  count: number;
};
